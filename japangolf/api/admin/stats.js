import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); export default async function handler(req,res){ try{ const {data:inquiries}=await supabase.from('inquiries').select('*'); const today=new Date().toISOString().slice(0,10); const stats={total:inquiries.length,today:inquiries.filter(i=>i.created_at?.slice(0,10)===today).length,new:inquiries.filter(i=>i.status==='new').length,failed:inquiries.filter(i=>!i.email_sent).length}; return res.status(200).json({success:true,stats}); }catch(e){ return res.status(500).json({error:e.message}) } }